import BigNumber from "bignumber.js";

/**
 * A string containing an integer.
 */
export type IntString = string;

export type numeric = number | string;

export type numeric2 = BigNumber | number | string;
